# Database — native

ObjectBox Java entities in `db/`. Follow [../../docs/PERSISTENCE.md](../../docs/PERSISTENCE.md).

Keep `:db:checkModelParity` green. Do not regenerate UIDs. Do not run `dart run build_runner`.

This file used to describe Dart ObjectBox in `lib/database/io/`. Those rules are void.
