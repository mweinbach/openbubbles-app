# Frontend — native

Compose UI lives in `app-native/` (desktop in `desktopApp/`). Follow [../../AGENTS.md](../../AGENTS.md)
and [../../docs/UI.md](../../docs/UI.md).

Load `.agents/skills/m3-expressive/SKILL.md` for visual work.

This file used to describe Flutter GetX widgets. Those rules are void.
