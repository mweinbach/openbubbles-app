// Re-exported from the shared helpers layer so that the database model layer
// can also import ChatServiceType without a layering violation.
export 'package:bluebubbles/helpers/types/classes/chat_service_type.dart';
