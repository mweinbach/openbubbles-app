# helpers/types/classes/ — Typed Helper Classes

Utility classes that don't belong to the DTO or extension layer.

## Files

| File | Purpose |
|------|---------|
| `chat_service_type.dart` | `ChatServiceType` enum — values: `iMessage`, `SMS`, `RCS`; used when creating chats and filtering handles |
| `language_codes.dart` | ISO 639-1 language code ↔ display name mapping; used by ML Kit language identification and Smart Reply |

## Related
- Extension methods: `../extensions/extensions.dart`
- Type helpers: `../helpers/CLAUDE.md`
