<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!-- SIM SMS/MMS: receive + send over the modem; READ_SMS backs MMS ingest
         (provider rows written by the system/default SMS app) and telephony
         thread-id tracking. RECEIVE_MMS is a normal (install-time) permission. -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_MMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.WRITE_SMS" />

    <!-- Self-update (GitHub Releases feed + PackageInstaller in-place
         install of this same package). The user grants the per-app
         "Install unknown apps" toggle on first use. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Camera and telephony features are optional so tablets, Chromebooks,
         and Wi-Fi-only devices remain installable. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <application
        android:name="app.openbubbles.nativeapp.OpenBubblesApplication"
        android:label="OpenBubbles"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.OpenBubblesNative"
        android:allowBackup="false"
        android:supportsRtl="true"
        android:enableOnBackInvokedCallback="true">

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/attachment_file_paths" />
        </provider>

        <activity
            android:name="app.openbubbles.nativeapp.NativeMainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SENDTO" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="sms" />
                <data android:scheme="smsto" />
                <data android:scheme="mms" />
                <data android:scheme="mmsto" />
            </intent-filter>
        </activity>

        <service
            android:name="app.openbubbles.nativeapp.service.NativePushService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Persistent Apple push (APNs) connection for iMessage" />
        </service>

        <receiver
            android:name="app.openbubbles.nativeapp.service.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Notification actions (explicit PendingIntents only, not exported) -->
        <receiver
            android:name="app.openbubbles.nativeapp.service.ReplyReceiver"
            android:exported="false" />
        <receiver
            android:name="app.openbubbles.nativeapp.service.MarkReadReceiver"
            android:exported="false" />

        <!-- PackageInstaller commit results for the self-updater. Only the
             system holds the PendingIntent that targets this action. -->
        <receiver
            android:name="app.openbubbles.nativeapp.update.UpdateInstallReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="app.openbubbles.nativeapp.action.UPDATE_INSTALL_RESULT" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="app.openbubbles.nativeapp.facetime.FaceTimeActionReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- SIM SMS/MMS receive + send-status (blocks adapted from the Flutter
             app's SMSReceiver registration; data-SMS/OTP receivers stay with
             the login flow and are intentionally NOT registered here). -->
        <receiver
            android:name="app.openbubbles.nativeapp.sms.SmsReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter android:priority="999">
                <action android:name="android.provider.Telephony.SMS_DELIVER" />
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="app.openbubbles.nativeapp.sms.MmsReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_WAP_PUSH">
            <intent-filter>
                <action android:name="android.provider.Telephony.WAP_PUSH_RECEIVED" />
                <data android:mimeType="application/vnd.wap.mms-message" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="app.openbubbles.nativeapp.sms.MmsPushReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_WAP_PUSH">
            <intent-filter>
                <action android:name="android.provider.Telephony.WAP_PUSH_DELIVER" />
                <data android:mimeType="application/vnd.wap.mms-message" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="app.openbubbles.nativeapp.sms.CarrierMmsReceivedReceiver"
            android:exported="false"
            android:taskAffinity="com.klinker.android.messaging.MMS_RECEIVED">
            <intent-filter>
                <action android:name="com.klinker.android.messaging.MMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="app.openbubbles.nativeapp.sms.SmsSendStatusReceiver"
            android:exported="false" />

        <service
            android:name="app.openbubbles.nativeapp.sms.RespondViaMessageService"
            android:exported="true"
            android:permission="android.permission.SEND_RESPOND_VIA_MESSAGE">
            <intent-filter>
                <action android:name="android.intent.action.RESPOND_VIA_MESSAGE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="sms" />
                <data android:scheme="smsto" />
                <data android:scheme="mms" />
                <data android:scheme="mmsto" />
            </intent-filter>
        </service>

        <service
            android:name="com.android.mms.transaction.TransactionService"
            android:exported="false" />

        <activity
            android:name="app.openbubbles.nativeapp.facetime.FaceTimeActivity"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation"
            android:exported="false"
            android:showOnLockScreen="true"
            android:supportsPictureInPicture="true"
            android:taskAffinity=".FaceTime"
            android:theme="@style/Theme.Call" />

        <service
            android:name="app.openbubbles.nativeapp.facetime.FaceTimeInCallService"
            android:exported="false"
            android:foregroundServiceType="camera|microphone" />

        <activity
            android:name="app.openbubbles.nativeapp.credentials.CredentialCreateActivity"
            android:taskAffinity=".CredentialCreate"
            android:theme="@style/Theme.Material3.DayNight.Dialog.Alert"
            tools:targetApi="34" />

        <activity
            android:name="app.openbubbles.nativeapp.credentials.CredentialGetActivity"
            android:taskAffinity=".CredentialGet"
            android:theme="@style/Theme.Material3.DayNight.Dialog.Alert"
            tools:targetApi="34" />

        <activity
            android:name="app.openbubbles.nativeapp.credentials.CredentialSettingsActivity"
            android:taskAffinity=".CredentialSettings"
            android:theme="@style/Theme.Material3.DayNight.Dialog.Alert" />

        <service
            android:name="app.openbubbles.nativeapp.credentials.CredentialService"
            android:enabled="true"
            android:exported="true"
            android:icon="@mipmap/ic_launcher"
            android:label="iCloud Keychain"
            android:permission="android.permission.BIND_CREDENTIAL_PROVIDER_SERVICE"
            tools:targetApi="34">
            <intent-filter>
                <action android:name="android.service.credentials.CredentialProviderService" />
            </intent-filter>
            <meta-data
                android:name="android.credentials.provider"
                android:resource="@xml/provider" />
        </service>

        <service
            android:name="app.openbubbles.nativeapp.credentials.OBAutofillService"
            android:exported="true"
            android:label="iCloud Keychain"
            android:permission="android.permission.BIND_AUTOFILL_SERVICE"
            tools:targetApi="27">
            <intent-filter>
                <action android:name="android.service.autofill.AutofillService" />
            </intent-filter>
            <meta-data
                android:name="android.autofill"
                android:resource="@xml/autofill_service" />
        </service>
    </application>

</manifest>
